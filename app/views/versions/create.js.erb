//At this state it does not exist so its id will be the revision number?
var position = $(".versions ul").children("#revision_<%= @version.revision_number %>").index(); //Find the position of the guy we're replacing
console.log("Calling version/create.js <%= @version.revision_number %>");
var renderedUI = "<%= j render 'versions/version', revision: @rev %>";
$(".versions ul").children("#revision_<%= @version.revision_number %>").remove(); //Remove the guy with the revision id
$(".versions ul li:eq(" + (parseInt(position) - 1) + ")").after(renderedUI); //Put in a new guy! It's ID will be the actual id
