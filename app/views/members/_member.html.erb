<li class="inset green">
  <% if @project %>
    <div> <%= link_to member.name, project_member_url(@project, member) %> </div>
    <div> Submitted <%= member.files_in(@project).count %> files </div>
  <% end %>
</li>
